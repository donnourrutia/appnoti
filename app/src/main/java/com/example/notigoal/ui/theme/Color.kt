package com.example.notigoal.ui.theme

import androidx.compose.ui.graphics.Color

// COLORES PARA EL TEMA OSCURO (estilo OneFootball)

// Tu color morado principal. Lo usaremos para títulos, marcadores y acentos.
val PurpleBrand = Color(0xFF9B59B6)

// Un gris muy oscuro para el fondo principal de la app. No es negro puro.
val DarkBackground = Color(0xFF121212)

// Un gris un poco más claro para las tarjetas (los elementos de la lista).
val DarkSurface = Color(0xFF1E1E1E)

// Texto principal (nombres de equipos, etc.). Blanco con un poco de transparencia.
val TextPrimaryDark = Color(0xDEFFFFFF) // 87% de opacidad

// Texto secundario (nombre de la liga, estado "Final", etc.).
val TextSecondaryDark = Color(0x99FFFFFF) // 60% de opacidad

// Color para elementos "en vivo". Un verde brillante.
val LiveGreen = Color(0xFF00C853)
